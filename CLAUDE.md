# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is newly initialized and currently empty. Update this file as the project takes shape.

## Getting Started

Add build, lint, and test commands here once tooling is set up. For example:

```
# Install dependencies
# npm install / pip install -r requirements.txt / etc.

# Run tests
# npm test / pytest / etc.

# Lint
# npm run lint / ruff check . / etc.
```

## Architecture

Document the high-level architecture here as the codebase grows — key modules, data flow, notable design decisions, and anything non-obvious that would take time to re-derive from reading the code alone.
